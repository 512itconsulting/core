package:
  # Keep the package name stable for existing dbpm artifact consumption.
  name: core
  version: "3.4.1"
  description: dbpm-core Oracle deployment metadata and lifecycle substrate
  vendor: rsantmyer
  license: Apache-2.0

database:
  platform: oracle
  minimum_version: "19c"

dependencies: []

scripts:
  install: Deployment_Manifests/deploy.sql
  upgrade: Deployment_Manifests/update.sql
